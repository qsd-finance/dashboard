#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@aragon/ui/bin/copy-aragon-ui-assets" "$@"
  ret=$?
else 
  node  "$basedir/../@aragon/ui/bin/copy-aragon-ui-assets" "$@"
  ret=$?
fi
exit $ret
